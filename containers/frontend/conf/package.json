{
  "name": "ft_pong",
  "version": "1.0.0",
  "main": "dist/server.js",
  "scripts": {
    "clean": "rm -rf dist && rm -rf src/public/script/* && rm -rf node_modules/",
    "build:backend": "tsc -p tsconfig.backend.json",
    "build:frontend": "tsc -p tsconfig.frontend.json",
    "build": "npm run build:backend && npm run build:frontend",
    "start": "nodemon"
  },
  "keywords": [],
  "author": "test",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@fastify/static": "^8.1.1",
    "@fastify/view": "^11.0.0",
    "@types/ejs": "^3.1.5",
    "ejs": "^3.1.10",
    "fastify": "^5.2.1",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
